# Copy to .env and fill in to enable live mode.
# Mock mode (default) needs nothing — the app runs fully offline.
LLM_MODE=mock            # mock | live
ANTHROPIC_API_KEY=       # required for live mode
CLAUSE_MODEL=claude-sonnet-4-5
